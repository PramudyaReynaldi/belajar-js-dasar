// ===================================== While loop ============================================

// ? while (kondisi) {
        // Pernyataan/perintah
// ? }

let text = "";
let i = 1;
while (i <= 5) {
    text += "\n The number is " + i;
    i++;
}
console.log(text);
